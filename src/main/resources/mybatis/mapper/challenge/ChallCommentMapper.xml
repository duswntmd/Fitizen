<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sku.fitizen.mapper.challenge.challCommentMapper">

    <!-- 댓글 목록 불러오기: 챌린지별 댓글 -->
    <select id="challCommentList" parameterType="com.sku.fitizen.domain.ChallComment"
                resultType="com.sku.fitizen.domain.ChallComment">
                select * FROM challComment
                where CHALLENGEID =#{challengId}
                ORDER BY commentDate
    </select>
</mapper>
